apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "operator-with-empty-nodeselector.fullname" . }}-configmap-vars
  labels:
  {{- include "operator-with-empty-nodeselector.labels" . | nindent 4 }}
data:
  VAR4: {{ .Values.configmapVars.var4 | quote }}