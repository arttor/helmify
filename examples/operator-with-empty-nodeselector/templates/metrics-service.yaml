apiVersion: v1
kind: Service
metadata:
  name: {{ include "operator-with-empty-nodeselector.fullname" . }}-controller-manager-metrics-service
  labels:
    control-plane: controller-manager
  {{- include "operator-with-empty-nodeselector.labels" . | nindent 4 }}
spec:
  type: {{ .Values.metricsService.type }}
  selector:
    control-plane: controller-manager
  {{- include "operator-with-empty-nodeselector.selectorLabels" . | nindent 4 }}
  ports:
	{{- .Values.metricsService.ports | toYaml | nindent 2 -}}